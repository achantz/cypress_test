{
  "name": "cypress_test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cypress": "yarn run cy:run",
    "cy:run": "cypress run --spec 'cypress/integration/sample_spec.js' --env URL=${URL}"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^8.3.1"
  }
}
