{
  "name": "cypress_test",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "cypress": "npm run cy:run",
    "cy:run": "cypress run --spec 'cypress/integration/sample_spec.js' --env URL=${URL}"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^8.3.1"
  }
}
